# НавесПро

Прототип сайта-визитки с калькулятором стоимости навесов.

## Запуск

```bash
npm install
npm run dev
```

Обычная страница открывается на `/`.

Админка прайса открывается на `/?admin=1`. Изменения сохраняются в браузере и сразу влияют на расчет.
